Mailr.SettingsRoute = Ember.Route.extend(
  # model: () ->
  #   return ['s','b','aa']

  # setupController: (controller, model) ->
  #   @controllerFor('folders').set('model', model)

  # renderTemplate: () ->
  #   @render('folders',
  #     controller: 'folders'
  #     outlet: 'sidebar'
  #   )
  #   @render('settings')
)

