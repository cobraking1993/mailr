<ul class="side-nav">
  {{#each folder in controller.system}}
  {{partial 'folderListItem'}}
  {{/each}}
</ul>

  {{#if noSystemCount}}
  <hr/>
  {{/if}}

<ul class="side-nav">
  {{#each folder in controller.noSystem}}
  {{partial 'folderListItem'}}
  {{/each}}
</ul>
